# `examples/` — runnable end-to-end examples

| Folder | What it does |
|--------|--------------|
| [`minimal_classification/`](minimal_classification/) | **~5 min end-to-end demo**: 2 × 2 XOR (4 samples) through `simulator/run_sweep.py` → `simulator/build_cube.py` → top-K MI + LinearSVC + leave-one-out CV. Demonstrates the full SWRC pipeline correctness on a single GPU. |

Each subfolder is self-contained — its `README.md` is the place to start.
